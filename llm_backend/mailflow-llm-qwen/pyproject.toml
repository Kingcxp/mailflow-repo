[project]
name = "mailflow-llm-qwen"
version = "0.1.0"
description = "MailFlow LLM backend: Alibaba Qwen (OpenAI-compatible chat completions)"
requires-python = ">=3.11"
dependencies = ["mailflow-core"]

[project.entry-points."mailflow.plugins"]
mailflow-llm-qwen = "mailflow_llm_qwen.plugin:plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mailflow_llm_qwen"]
