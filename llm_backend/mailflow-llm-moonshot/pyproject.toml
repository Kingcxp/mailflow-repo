[project]
name = "mailflow-llm-moonshot"
version = "0.1.0"
description = "MailFlow LLM backend: Moonshot AI (OpenAI-compatible chat completions)"
requires-python = ">=3.11"
dependencies = ["mailflow-core"]

[project.entry-points."mailflow.plugins"]
mailflow-llm-moonshot = "mailflow_llm_moonshot.plugin:plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mailflow_llm_moonshot"]
