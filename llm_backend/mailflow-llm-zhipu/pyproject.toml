[project]
name = "mailflow-llm-zhipu"
version = "0.1.0"
description = "MailFlow LLM backend: Zhipu GLM (OpenAI-compatible chat completions)"
requires-python = ">=3.11"
dependencies = ["mailflow-core"]

[project.entry-points."mailflow.plugins"]
mailflow-llm-zhipu = "mailflow_llm_zhipu.plugin:plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mailflow_llm_zhipu"]
