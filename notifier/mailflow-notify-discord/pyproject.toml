[project]
name = "mailflow-notify-discord"
version = "0.1.0"
description = "Push mail alerts to a Discord webhook"
requires-python = ">=3.11"
dependencies = ["mailflow-core"]

[project.entry-points."mailflow.plugins"]
mailflow-notify-discord = "mailflow_notify_discord.plugin:plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mailflow_notify_discord"]
