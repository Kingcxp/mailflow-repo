<!-- Generated from plugin.json by tools/gen_plugin_readmes.py. Edit plugin.json. -->

# DingTalk Notifier

Registers the notifier component id `dingtalk`. Options: `access_token` (robot webhook token) or `webhook_url` (full URL), optional `secret` (sign), `timeout_seconds`. Sends `msgtype=text` with the mail summary.

---

<!-- zh-CN -->

# DingTalk Notifier

Registers the notifier component id `dingtalk`. Options: `access_token` (robot webhook token) or `webhook_url` (full URL), optional `secret` (sign), `timeout_seconds`. Sends `msgtype=text` with the mail summary.

---

Metadata: [`plugin.json`](plugin.json) · Marketplace: [https://github.com/Kingcxp/mailflow-repo/tree/main/notifier/mailflow-notify-dingtalk](https://github.com/Kingcxp/mailflow-repo/tree/main/notifier/mailflow-notify-dingtalk)
