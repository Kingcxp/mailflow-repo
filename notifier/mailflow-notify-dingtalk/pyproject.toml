[project]
name = "mailflow-notify-dingtalk"
version = "0.1.0"
description = "Push mail alerts to a DingTalk group robot webhook"
requires-python = ">=3.11"
dependencies = ["mailflow-core"]

[project.entry-points."mailflow.plugins"]
mailflow-notify-dingtalk = "mailflow_notify_dingtalk.plugin:plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mailflow_notify_dingtalk"]
