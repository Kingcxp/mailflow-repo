{
  "id": "mailflow-mail-outlook",
  "name": "Outlook Mail Source",
  "version": "0.1.0",
  "description": "IMAP/SMTP mail source for Outlook/Office 365 with app-password auth",
  "categories": [
    "mail_source"
  ],
  "package": "mailflow-mail-outlook",
  "source": "git+https://github.com/Kingcxp/mailflow-repo.git#subdirectory=mail_source/mailflow-mail-outlook",
  "author": "MailFlow community",
  "license": "MIT",
  "homepage": "https://github.com/Kingcxp/mailflow-repo/tree/main/mail_source/mailflow-mail-outlook",
  "readme": "# Outlook Mail Source\n\nRegisters the mail source component id `outlook`. IMAP to outlook.office365.com:993 (SSL), SMTP via smtp.office365.com:587 (STARTTLS). Options: `username` (full email address), `password` (**an app password, not your account password** — enable MFA then create one under Microsoft Account → Security → App passwords), `imap_folder` (default INBOX), `interval_seconds` (default 300), `limit` (default 20), `analyze_backlog` (default false). Native OAuth is planned; today the adapter uses IMAP with an app password.\n",
  "descriptions": {
    "zh-CN": "IMAP/SMTP mail source for Outlook/Office 365 with app-password auth"
  },
  "readmes": {
    "zh-CN": "# Outlook Mail Source\n\nRegisters the mail source component id `outlook`. IMAP to outlook.office365.com:993 (SSL), SMTP via smtp.office365.com:587 (STARTTLS). Options: `username` (full email address), `password` (**an app password, not your account password** — enable MFA then create one under Microsoft Account → Security → App passwords), `imap_folder` (default INBOX), `interval_seconds` (default 300), `limit` (default 20), `analyze_backlog` (default false). Native OAuth is planned; today the adapter uses IMAP with an app password.\n"
  }
}