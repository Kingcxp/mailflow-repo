<!-- Generated from plugin.json by tools/gen_plugin_readmes.py. Edit plugin.json. -->

# Rule Filter Processor

Registers the processor component id `filter`. Options: `senders` (list), `subjects` (list of substrings), `keywords` (list of body substrings). Matches are classified `ad`.

---

<!-- zh-CN -->

# Rule Filter Processor

Registers the processor component id `filter`. Options: `senders` (list), `subjects` (list of substrings), `keywords` (list of body substrings). Matches are classified `ad`.

---

Metadata: [`plugin.json`](plugin.json) · Marketplace: [https://github.com/Kingcxp/mailflow-repo/tree/main/processor/mailflow-processor-filter](https://github.com/Kingcxp/mailflow-repo/tree/main/processor/mailflow-processor-filter)
